# Default values for db2-schema.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

jobName: "cp-db2schema-job"
ConfigmapName: "cp-db2schema-configmap"

storageClassName: ""
